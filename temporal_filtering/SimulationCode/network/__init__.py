"""Connectome substrate: connectivity backends, network construction, column
layout helpers, and the FAFB sys.path bootstrap.

Import submodules directly, e.g. ``from network.connectivity import ScatterConn``
or ``from network import bootstrap  # noqa`` to put FAFBv783 on ``sys.path``.
Training targets now live in the ``stimulus`` package, not here.
"""
